name = "投资 – 收盘复盘（只读）"
base = "logs"
input = "INVEST_LOG.md"
semantic_kind = "invest_log"
empty_message = "（暂无投资日志，请编辑 workspace/logs/INVEST_LOG.md）"
header = "--- 收盘复盘（只读） ---"
footer = "(已写入 runtime-audit.log，source=invest_close)"
max_lines = 20

## 说明：
## - 用于在收盘后基于 INVEST_LOG.md 做一次简单复盘摘要。
## - 与 invest_morning 共用同一日志文件，但关注的时间段和解读方式可以不同。


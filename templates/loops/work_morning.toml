name = "工作 – 早晨计划（只读）"
base = "workspace"
input = "today_tasks.md"
semantic_kind = "work_log"
empty_message = "（暂无今日任务，请编辑 workspace/today_tasks.md）"
header = "--- 今日计划（只读） ---"
footer = "(已写入 runtime-audit.log，source=work_morning)"
join_separator = "；"
max_lines = 0

## 说明：
## - 该 Loop 会从 workspace/today_tasks.md 读取非空行，按顺序拼成「今日计划」摘要。
## - 建议你每天早上先编辑 today_tasks.md，再运行：
##     openpup run work:morning

